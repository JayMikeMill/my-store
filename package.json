{
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "concurrently": "^9.2.1",
    "jest": "^30.1.3",
    "prettier": "^3.6.2",
    "ts-jest": "^29.4.4",
    "typescript": "^5.9.2"
  },
  "name": "my-project",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build-types": "npm --prefix backend run prisma:generate",
    "prisma-push": "npm --prefix backend run prisma:push",
    "prisma-migrate": "npm --prefix backend run prisma:migrate",
    "test": "jest"
  },
  "dependencies": {
    "react-colorful": "^5.6.1"
  }
}
